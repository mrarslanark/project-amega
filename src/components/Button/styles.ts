import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  primary: {
    padding: 12,
    borderRadius: 12,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: 'rgba(8, 99, 117, 0.1)',
  },
  secondary: {
    padding: 12,
    borderColor: '#086375',
    backgroundColor: 'rgba(8, 99, 117, 0.1)',
    borderWidth: 2,
    borderRadius: 12,
    justifyContent: 'center',
    alignItems: 'center',
  },
  tertiary: {
    padding: 12,
    backgroundColor: '#086375',
    borderRadius: 12,
    justifyContent: 'center',
    alignItems: 'center',
  },
  disabled: {
    padding: 12,
    borderRadius: 12,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: 'rgba(0, 0, 0, 0.05)',
  },
});
