import React from 'react';
import {Text, View} from 'react-native';

const Detail: React.FC = (): React.JSX.Element => {
  return (
    <View>
      <Text>Detail Screen</Text>
    </View>
  );
};

export default Detail;
