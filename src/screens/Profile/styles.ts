import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    padding: 16,
    flex: 1,
    backgroundColor: 'white',
    rowGap: 12,
  },
  image: {
    width: '100%',
    height: '50%',
    borderRadius: 12,
  },
});
